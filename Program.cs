using Day_5_assignment;

namespace Day5_Assign
{
    internal class Program
    {
        static void Main(string[] args)
        {

            /**int choice = Convert.ToInt16(Console.ReadLine());
            Console.WriteLine("how many elements to add in array ");
            int size = Convert.ToInt32(Console.ReadLine());
            switch (choice)
            {
                case 1:
                    int[] arr = new int[size];
                    Console.WriteLine("plz enter element ");
                    for (int i = 0; i < arr.Length; i++)
                    {
                        arr[i] = Convert.ToInt32(Console.ReadLine());
                    }
                    Console.WriteLine("plz  pick any one element from this array");
                    int delete = Convert.ToInt32(Console.ReadLine());
                    VarIntDelete.Delete(arr, delete);
                    break;
                case 2:
                    double[] dArr = new double[size];
                    Console.WriteLine("plz enter element ");

                    for (int i = 0; i < dArr.Length; i++)
                    {
                        dArr[i] = Convert.ToDouble(Console.ReadLine());
                    }
                    Console.WriteLine("plz  pick any one element from this array");
                    double deleteDouble = Convert.ToInt32(Console.ReadLine());
                    VarIntDelete.Delete(dArr, deleteDouble);
                    break;
                case 3:
                     char[] drr = new char[size];
                    Console.WriteLine("plz enter element ");

                    for (int i = 0; i < drr.Length; i++)
                    {
                        drr[i] = Console.ReadLine()[0];
                    }
                    Console.WriteLine("plz  pick any one element from this array");
                    char deletechar = Console.ReadLine()[0];
                    VarIntDelete.Delete(drr, deletechar);
                    break;

            }
            */

            /*

            int choice = Convert.ToInt16(Console.ReadLine());
            Console.WriteLine("how many elements to add in array ");
            int size = Convert.ToInt32(Console.ReadLine());
            switch (choice)
            {
                case 1:
                    int[] arr = new int[size];
                    Console.WriteLine("plz enter element ");
                    for (int i = 0; i < arr.Length; i++)
                    {
                        arr[i] = Convert.ToInt32(Console.ReadLine());
                    }
                    Console.WriteLine("plz  pick any one element from this array");
                    int deletes = Convert.ToInt32(Console.ReadLine());
                    Q2Genric<int> g = new Q2Genric<int>();
                    g.Delete(arr, deletes);
                    break;
                case 2:
                    double[] dArr = new double[size];
                    Console.WriteLine("plz enter element ");

                    for (int i = 0; i < dArr.Length; i++)
                    {
                        dArr[i] = Convert.ToDouble(Console.ReadLine());
                    }
                    Console.WriteLine("plz  pick any one element from this array");
                    double deleteDouble = Convert.ToInt32(Console.ReadLine());
                    Q2Genric<double> l = new Q2Genric<double>();
                    l.Delete(dArr, deleteDouble);
                    break;
                case 3:
                    char[] drr = new char[size];
                    Console.WriteLine("plz enter element ");

                    for (int i = 0; i < drr.Length; i++)
                    {
                        drr[i] = Console.ReadLine()[0];
                    }
                    Console.WriteLine("plz  pick any one element from this array");
                    char deletechar = Console.ReadLine()[0];
                    Q2Genric<char> m = new Q2Genric<char>();    
                    m.Delete(drr, deletechar);
                    break;
            */

                  // FindMinimum.Minimum();
                //  FloatMinimum.MinimumFloat();
                    

            


        }
    }
}